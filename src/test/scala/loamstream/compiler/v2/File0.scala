package loamstream.compiler.v2

object File0 extends loamstream.compiler.v2.LoamFile {
  import loam._
  
  /*val in = store("a.txt").asInput
  val out = store("b.txt")
  
  cmd"cp $in $out".in(in).out(out)*/
  
  logFooter()
}
