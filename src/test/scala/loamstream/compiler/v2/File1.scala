package loamstream.compiler.v2

object File1 extends loamstream.compiler.v2.LoamFile {
  import loam._
  
 /* val in = File0.out
  val out = store("target/c.txt")
  
  cmd"cp $in $out".in(in).out(out)*/
  
  logFooter()
}
